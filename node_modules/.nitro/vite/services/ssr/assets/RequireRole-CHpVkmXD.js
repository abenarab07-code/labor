import { t as useAdminAuth } from "./auth-BoyH9ZGt.js";
import { Link } from "@tanstack/react-router";
import { Fragment, jsx, jsxs } from "react/jsx-runtime";
import { ShieldAlert } from "lucide-react";
//#region src/components/admin/RequireRole.tsx
/**
* Client-side route guard for admin sub-pages.
* Blocks rendering (and therefore data fetching in child components) until
* session/role resolves. Database RLS remains the source of truth — this is
* defense-in-depth so unauthorized users never see the surface.
*/
function RequireRole({ roles, children }) {
	const auth = useAdminAuth();
	if (auth.status === "loading") return /* @__PURE__ */ jsx("div", {
		className: "p-10 text-sm text-ink/50",
		children: "Vérification des autorisations…"
	});
	if (!auth.roles.some((r) => roles.includes(r))) return /* @__PURE__ */ jsxs("div", {
		className: "max-w-lg mx-auto mt-16 rounded-2xl bg-white border border-black/5 p-8 text-center",
		children: [
			/* @__PURE__ */ jsx("div", {
				className: "h-12 w-12 rounded-full bg-red-50 text-red-600 flex items-center justify-center mx-auto mb-4",
				children: /* @__PURE__ */ jsx(ShieldAlert, { className: "h-6 w-6" })
			}),
			/* @__PURE__ */ jsx("h2", {
				className: "font-serif text-xl text-petrol mb-2",
				children: "Accès refusé"
			}),
			/* @__PURE__ */ jsx("p", {
				className: "text-sm text-ink/60 mb-6",
				children: "Cette section est réservée aux administrateurs du cabinet. Contactez un administrateur si vous pensez qu'il s'agit d'une erreur."
			}),
			/* @__PURE__ */ jsx(Link, {
				to: "/admin",
				className: "inline-flex items-center px-4 py-2 rounded-full bg-petrol text-ivory text-sm",
				children: "Retour au tableau de bord"
			})
		]
	});
	return /* @__PURE__ */ jsx(Fragment, { children });
}
var RequireAdmin = ({ children }) => /* @__PURE__ */ jsx(RequireRole, {
	roles: ["admin"],
	children
});
//#endregion
export { RequireAdmin as t };
