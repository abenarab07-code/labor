import { t as supabase } from "./client-C6ZaJElq.js";
import { f as formatDateTime } from "./utils-15guiZLl.js";
import { i as PageHeader, r as EmptyState } from "./AdminShell-CWUY70tp.js";
import { t as RequireAdmin } from "./RequireRole-CHpVkmXD.js";
import { useEffect, useState } from "react";
import { jsx, jsxs } from "react/jsx-runtime";
import { ScrollText } from "lucide-react";
//#region src/routes/admin.audit.tsx?tsr-split=component
function AuditPage() {
	const [items, setItems] = useState([]);
	useEffect(() => {
		supabase.from("audit_logs").select("*").order("created_at", { ascending: false }).limit(200).then(({ data }) => setItems(data ?? []));
	}, []);
	return /* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx(PageHeader, {
		title: "Journal d'audit",
		subtitle: "Traçabilité de toutes les actions sensibles"
	}), items.length === 0 ? /* @__PURE__ */ jsx(EmptyState, {
		title: "Aucun événement",
		icon: ScrollText
	}) : /* @__PURE__ */ jsx("div", {
		className: "rounded-2xl bg-white border border-black/5 overflow-hidden",
		children: /* @__PURE__ */ jsxs("table", {
			className: "w-full text-sm",
			children: [/* @__PURE__ */ jsx("thead", {
				className: "text-[10px] uppercase tracking-wider text-ink/50 border-b border-black/5",
				children: /* @__PURE__ */ jsxs("tr", { children: [
					/* @__PURE__ */ jsx("th", {
						className: "text-left px-4 py-3",
						children: "Date"
					}),
					/* @__PURE__ */ jsx("th", {
						className: "text-left",
						children: "Action"
					}),
					/* @__PURE__ */ jsx("th", {
						className: "text-left",
						children: "Entité"
					}),
					/* @__PURE__ */ jsx("th", {
						className: "text-left",
						children: "Résumé"
					})
				] })
			}), /* @__PURE__ */ jsx("tbody", { children: items.map((a) => /* @__PURE__ */ jsxs("tr", {
				className: "border-t border-black/5",
				children: [
					/* @__PURE__ */ jsx("td", {
						className: "px-4 py-3 whitespace-nowrap text-ink/60",
						children: formatDateTime(a.created_at)
					}),
					/* @__PURE__ */ jsx("td", {
						className: "text-petrol",
						children: a.action
					}),
					/* @__PURE__ */ jsx("td", {
						className: "text-ink/60",
						children: a.entity_type
					}),
					/* @__PURE__ */ jsx("td", {
						className: "text-ink",
						children: a.summary ?? "—"
					})
				]
			}, a.id)) })]
		})
	})] });
}
var SplitComponent = () => /* @__PURE__ */ jsx(RequireAdmin, { children: /* @__PURE__ */ jsx(AuditPage, {}) });
//#endregion
export { SplitComponent as component };
